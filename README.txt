가운데 정렬 align="center"


mdi-icon site
https://pictogrammers.github.io/@mdi/font/5.4.55/


vuetify
v-col cols="auto"  
자기 크기만큼만 차지


$posts = Post::orderby('created_at', 'desc')->limit(6)->get();
최근 게시글 6개만 가져옴

vue laravel 서버를 따로 사용 할 때 이미지를 화면에 출력하기 위해서는 form 안에 넣어서 보내야한다



TodoList

1. 그전에 했던거 보여주는 뷰
2. 


AWS
VPC를 만드면 라우팅테이블이 생성됨
내부통신만 가능한 정보만 들어있음

퍼블링서브넷이 사용할 라우팅테이블을 만듬
인터넷게이트웨이 연결을 추가해서 외부와 통신을 수행

1. 라우팅테이블 생성
이름과 해당 VPC를 선택
라우팅테이블 생성

기본적으로 설정을 안하면 내부통신만 가능한 경로만 있음 
외부와 통신이 가능하게 하려면 경로추가를 해야함
라우팅 편집 선택
대상 - 인터넷 게이트웨이

서브넷연결편집
퍼블릭서브넷의 라우팅테이블에 경로가 추가 되어있음

방화벽설정
보안그룹 
여기에는 인바운드규칙과 아웃바운드 규칙이 있음
인바운드는 다 차단함
아웃바운드는 다 허용함

외부 사용자 접근을 허락하기 위해 
인바운드 규칙에 HTTP의 접근을 허락한다
HTTPS의 접근도 허락

데이터베이스를 보호하는 방화벽

RDS

엔진옵션 선택 버전도 
DB인스턴스 식별자 DB이름 작성
해당 VPC선택
아까만든 보안그룹을 선택

EC2인스턴스 생성


데이터베이스에 접근  rds에 접근

데이터테이블 생성 회원가입이나 포스트테이블



RDS
